package br.unifor.Panels;

import java.awt.Color;
import java.awt.Graphics;

/**
 * 
 *  Essa classe desenha a prévia da próxima peça
 *
 */


public class pCanvas extends javax.swing.JPanel implements IPainel {

	  private pCanvas(){
		  config();
	  }
	  
	  private static pCanvas instance = null;
	  public static pCanvas getInstance(){
		  return instance == null ? instance = new pCanvas() : instance;
	  }
	  
	@Override
	public void config() {
		
		this.setBackground(Color.orange.brighter());
	}
	
	@Override
	public void paint(Graphics g) {
		// desenhar a próxima figura
	}
}
